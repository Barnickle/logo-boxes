extends layout

block content
  section.entries
    each entry in entries
      article(
        data-hueusername=entry.datahue?entry.datahue.username:null
        data-hueip=entry.datahue?entry.datahue.ip:null
        data-huepost=entry.datahue?entry.datahue.post:null
      )
        a(
          target="_blank"
          href=entry.action
          data-title=entry.title
        )
          img(
            src="/images/logos/"+entry.logo
            style=entry.style
          )
  section#windowApp
    transition(name='fade')
      .box(v-if='show')
        h1 {{title}}
        img(
          src='/images/close.svg'
          alt='close'
          @click='close'
        )
        iframe(
          v-if='show'
          :src='src'
        )
